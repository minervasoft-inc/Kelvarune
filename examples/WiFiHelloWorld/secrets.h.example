// このファイルをコピーして同じフォルダに secrets.h という名前で保存し、
// 実際のWi-Fi情報に書き換えてください。
// secrets.h はリポジトリの.gitignore対象のため、認証情報がコミットされることはありません。

#pragma once

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
